#include <bits/stdc++.h>

using namespace std;

int main() {
    // your code goes here
    int t;
    cin >> t;
    while (t--) {
        int n, x = 0, y = 0;
        cin >> n;
        for (int i = 0; i < n; i++) {
            int z;
            cin >> z;
            if (z % 2) {
                x++;
            }
            else {
                y++;
            }
        }
        cout << min(n, 2 * (min(x, y))+1) << endl;
    }
}