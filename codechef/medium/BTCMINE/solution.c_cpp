#include <bits/stdc++.h>

using namespace std;

int main() {
    // your code goes here
    int t;
    cin >> t;
    while (t--) {
        int x, y;
        cin >> x >> y;
        for (int i = 1; i <= 200; i++) {
            int a = 0;
            bool b = false;
            for(int j=1;j<=i;j++){
                a+= j*j;
                int c=y*(a+(i-j)*k*k);
                int d=k*x;
                if(c>d){
                    b=true;
                    break;
                }
            }
            if(b){
                cout<<i<<endl;
                break;
            }
        }
    }
}